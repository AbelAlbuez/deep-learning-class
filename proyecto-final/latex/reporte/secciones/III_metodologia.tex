\section{Metodología}

\subsection{Métricas de Evaluación}
Para evaluar las arquitecturas propuestas se emplea un conjunto
de métricas estándar de clasificación multiclase:

\begin{itemize}
  \item \textbf{Accuracy:} proporción global de aciertos,
        $\text{Acc} = (TP+TN)/(TP+TN+FP+FN)$.
  \item \textbf{Precision (macro):} promedio no ponderado de la
        precisión por clase,
        $\tfrac{1}{C}\sum_{c} TP_c/(TP_c+FP_c)$.
  \item \textbf{Recall (macro):} promedio no ponderado de la
        sensibilidad por clase,
        $\tfrac{1}{C}\sum_{c} TP_c/(TP_c+FN_c)$.
  \item \textbf{F1-Score (macro):} media armónica de Precision y
        Recall, $F1 = 2 \cdot P \cdot R / (P+R)$, calculada por
        clase y promediada de forma no ponderada.
  \item \textbf{Matriz de confusión:} análisis cualitativo de
        los errores entre pares de clases.
\end{itemize}

Dado el desbalance descrito en la Sección~\ref{sec:eda}, la
\textit{Accuracy} puede ser engañosa, pues un modelo que ignore
por completo la clase \textit{no\_tumor} aún alcanzaría valores
razonables. El \textbf{F1-Score macro} otorga el mismo peso a
todas las clases sin considerar su frecuencia, por lo que
penaliza explícitamente los modelos que descuidan las clases
minoritarias. Por esta razón se adopta como métrica principal
de comparación entre los tres modelos.

\subsection{Arquitectura Modelo 1 --- CNN Baseline}
\label{sec:arq-m1}

La arquitectura del Modelo~1 fue diseñada como un \textit{baseline}
convolucional ligero, inspirado en la red OkanNet propuesta por
Uçar y Kurt~\cite{okannet2026} y en la CNN desde cero de Gupta
\textit{et al.}~\cite{gupta2023}. Se compone de tres bloques
convolucionales secuenciales seguidos de un cabezal denso, sin
emplear \textit{Batch Normalization} ni conexiones residuales, de
modo que sirva como punto de referencia frente a los Modelos~2
y~3.

\begin{table}[!t]
\centering
\caption{Arquitectura del Modelo~1 (CNN Baseline). Entrada
$(B, 3, 224, 224)$.}
\label{tab:arq-m1}
\renewcommand{\arraystretch}{1.15}
\setlength{\tabcolsep}{4pt}
\footnotesize
\begin{tabular}{@{}clccccr@{}}
\toprule
\textbf{\#} & \textbf{Capa} & \textbf{Filt.} & \textbf{Kernel} &
\textbf{Stride} & \textbf{Salida $(C,H,W)$} & \textbf{Params} \\
\midrule
1  & Conv2D       & 16 & $3{\times}3$ & 1  & $(16,224,224)$ & $448$         \\
2  & ReLU         & -- & --           & -- & $(16,224,224)$ & $0$           \\
3  & MaxPool2D    & -- & $2{\times}2$ & 2  & $(16,112,112)$ & $0$           \\
4  & Conv2D       & 32 & $3{\times}3$ & 1  & $(32,112,112)$ & $4\,640$      \\
5  & ReLU         & -- & --           & -- & $(32,112,112)$ & $0$           \\
6  & MaxPool2D    & -- & $2{\times}2$ & 2  & $(32,56,56)$   & $0$           \\
7  & Conv2D       & 64 & $3{\times}3$ & 1  & $(64,56,56)$   & $18\,496$     \\
8  & ReLU         & -- & --           & -- & $(64,56,56)$   & $0$           \\
9  & MaxPool2D    & -- & $2{\times}2$ & 2  & $(64,28,28)$   & $0$           \\
10 & Flatten      & -- & --           & -- & $(50\,176,)$   & $0$           \\
11 & Linear       & -- & --           & -- & $(128,)$       & $6\,422\,656$ \\
12 & ReLU         & -- & --           & -- & $(128,)$       & $0$           \\
13 & Dropout(0.5) & -- & --           & -- & $(128,)$       & $0$           \\
14 & Linear       & -- & --           & -- & $(4,)$         & $516$         \\
\midrule
\multicolumn{6}{r}{\textbf{Total parámetros entrenables:}} & $\mathbf{6\,446\,756}$ \\
\bottomrule
\end{tabular}
\end{table}

Las decisiones de diseño se justifican como sigue:

\begin{itemize}
  \item \textbf{Tres bloques convolucionales} con filtros
        $16{\to}32{\to}64$, replicando la progresión geométrica
        de OkanNet~\cite{okannet2026}. Esta profundidad limitada
        deja margen explícito para que los Modelos~2 y~3
        muestren mejoras atribuibles a su mayor complejidad.
  \item \textbf{Kernels} $3{\times}3$ con \textit{padding}
        \textit{same} y \textbf{MaxPool} $2{\times}2$ con
        \textit{stride}~2: configuración estándar en la
        literatura de clasificación MRI~\cite{gupta2023} que
        preserva la información espacial dentro de cada bloque
        y la reduce a la mitad entre bloques.
  \item \textbf{Sin \textit{Batch Normalization}}: decisión
        deliberada del enunciado, para aislar el efecto que
        introducirá el Modelo~2 al añadir BN.
  \item \textbf{Cabezal denso}: \texttt{Linear(50\,176, 128)}
        $\to$ \texttt{ReLU} $\to$ \texttt{Dropout(0{,}5)} $\to$
        \texttt{Linear(128, 4)}. El \textit{Dropout} en la única
        capa oculta del cabezal sigue la práctica de
        OkanNet~\cite{okannet2026} y mitiga el sobreajuste sin
        introducir regularización adicional.
  \item \textbf{Inicialización}: \textit{Kaiming Normal} en las
        capas \texttt{Conv2D} (modo \texttt{fan\_out}) y en las
        capas \texttt{Linear} (modo \texttt{fan\_in}). Esta
        separación es crucial: una inicialización
        \texttt{fan\_out} aplicada a
        \texttt{Linear(50\,176, 128)} produciría una magnitud
        de preactivaciones tan elevada que rompería la
        convergencia, especialmente cuando se introduce BN.
\end{itemize}

El número total de parámetros entrenables es de $6\,446\,756$,
dominado casi en su totalidad ($\approx 99{,}6$\,\%) por la
primera capa totalmente conectada, lo cual es un patrón típico
de arquitecturas tipo LeNet/AlexNet sin
\textit{Global Average Pooling}.

\subsubsection*{Arquitectura comparativa OkanNet}

Como contraste académico se implementó una versión \emph{réplica}
de OkanNet~\cite{okannet2026}, idéntica al Modelo~1 pero con
\textit{Batch Normalization 2D} añadido entre cada \texttt{Conv2D}
y su \texttt{ReLU}. El total de parámetros asciende a
$6\,446\,980$ ($+224$ por las medias y varianzas aprendibles de
BN), y los hiperparámetros de entrenamiento se mantienen
idénticos al \textit{baseline} para que la única variable que
cambia entre ambas redes sea precisamente la presencia de BN.

\subsection{Arquitectura Modelo 2 --- CNN Profunda con BN y Dropout}
\label{sec:arq-m2}

El Modelo~2 está motivado por una limitación específica del
Modelo~1 documentada en la Sección~\ref{sec:arq-m1}: la
versión comparativa OkanNet, que añade
\textit{Batch Normalization} sobre el baseline manteniendo el
resto de hiperparámetros, redujo el F1-macro de validación
en lugar de mejorarlo. El análisis sugiere que esta caída no
invalida BN como técnica, sino que evidencia su sensibilidad
al régimen de entrenamiento. El Modelo~2 retoma esa hipótesis
y la extiende: una red sustancialmente más profunda (10 capas
convolucionales frente a 3), con BN tras cada \texttt{Conv2D}
y \textit{Dropout2d} progresivo entre bloques, entrenada con
un régimen de hiperparámetros propio.

Adicionalmente se construye una segunda variante
(\textit{DeepCNN-BN-GAP}) que comparte el mismo \textit{backbone}
convolucional pero reemplaza el cabezal denso por
\textit{Global Average Pooling} (GAP) seguido de una única capa
\texttt{Linear}. Esta segunda red permite cuantificar el aporte
real del cabezal FC, una vez que el \textit{backbone} ya tiene
profundidad suficiente para producir representaciones
discriminativas.

\subsubsection*{Backbone convolucional}

Ambas variantes comparten un \textit{backbone} VGG-style
compuesto por cinco bloques, cada uno con dos capas
\texttt{Conv2D} de kernel $3{\times}3$ seguidas de
\texttt{BatchNorm2D}, \texttt{ReLU} y, salvo el último,
\texttt{MaxPool2D} $2{\times}2$. Cada bloque finaliza con
\texttt{Dropout2D} cuya tasa crece desde 0{,}10 hasta 0{,}30
conforme aumenta la profundidad. La progresión de filtros
$32{\to}64{\to}128{\to}256{\to}512$ duplica la capacidad
representacional en cada nivel, siguiendo el patrón clásico
de VGG~\cite{simonyan2014very}. El último bloque omite el
\texttt{MaxPool} de manera deliberada para que la salida sea
$(B, 512, 14, 14)$ en lugar de $(B, 512, 7, 7)$; esto permite
aplicar \texttt{AdaptiveAvgPool2D} $(2, 2)$ posteriormente
bajo el backend MPS de Apple, que requiere que el tamaño de
entrada sea divisible por el de salida (14 es divisible por
2, 7 no lo es).

\begin{table*}[!t]
\centering
\caption{\textit{Backbone} convolucional compartido por
DeepCNN-BN y DeepCNN-BN-GAP. Entrada $(B, 3, 224, 224)$.
Cada bloque ejecuta dos \texttt{Conv2D}-BN-ReLU consecutivas
antes del \texttt{MaxPool} y \texttt{Dropout2D}.}
\label{tab:arq-m2-backbone}
\renewcommand{\arraystretch}{1.15}
\setlength{\tabcolsep}{6pt}
\footnotesize
\begin{tabular}{@{}clcccr@{}}
\toprule
\textbf{\#} & \textbf{Bloque} & \textbf{Filtros} &
\textbf{Pool} & \textbf{Salida $(C,H,W)$} & \textbf{Params} \\
\midrule
1 & 2$\times$Conv-BN-ReLU + MaxPool + D2d(0{,}10) & 32  & sí & $(32, 112, 112)$ & $10\,272$ \\
2 & 2$\times$Conv-BN-ReLU + MaxPool + D2d(0{,}15) & 64  & sí & $(64,  56,  56)$ & $55\,680$ \\
3 & 2$\times$Conv-BN-ReLU + MaxPool + D2d(0{,}20) & 128 & sí & $(128, 28,  28)$ & $221\,952$ \\
4 & 2$\times$Conv-BN-ReLU + MaxPool + D2d(0{,}25) & 256 & sí & $(256, 14,  14)$ & $886\,272$ \\
5 & 2$\times$Conv-BN-ReLU + D2d(0{,}30)           & 512 & no & $(512, 14,  14)$ & $3\,542\,016$ \\
\midrule
\multicolumn{5}{r}{\textbf{Subtotal \textit{backbone}:}} & $\mathbf{4\,716\,192}$ \\
\bottomrule
\end{tabular}
\end{table*}

\subsubsection*{Cabezales: DeepCNN-BN vs DeepCNN-BN-GAP}

A la salida del \textit{backbone} $(B, 512, 14, 14)$ se conectan
dos cabezales distintos según la variante:

\paragraph*{Variante A --- DeepCNN-BN (cabezal FC)}
Aplica \texttt{AdaptiveAvgPool2D} $(2, 2)$ para reducir la
información espacial a $(B, 512, 2, 2)$, equivalente a 2{,}048
características al aplanar. Sobre ese vector se construye un
cabezal denso compacto: \texttt{Linear(2048, 256)} $\to$
\texttt{BatchNorm1D(256)} $\to$ \texttt{ReLU} $\to$
\texttt{Dropout(0{,}5)} $\to$ \texttt{Linear(256, 4)}. Este
diseño preserva la idea del baseline ---un cuello de botella
denso antes de la salida--- pero introduce dos regularizadores:
\textit{BatchNorm1D} estabiliza las activaciones a la entrada
de la \texttt{Linear} final, y \textit{Dropout(0{,}5)} previene
co-adaptación de neuronas.

\paragraph*{Variante B --- DeepCNN-BN-GAP (cabezal GAP)}
Aplica \texttt{AdaptiveAvgPool2D} $(1, 1)$, es decir,
\textit{Global Average Pooling} clásico, reduciendo cada uno
de los 512 mapas de activación a un escalar. El cabezal se
limita a \texttt{Dropout(0{,}5)} $\to$ \texttt{Linear(512, 4)}.
Esta elección sigue la línea de
NIN~\cite{lin2013network} y ResNet~\cite{he2016deep}:
sustituye el FC ``gigante'' por un promediado espacial que
fuerza al \textit{backbone} a producir mapas semánticamente
significativos, e introduce una regularización estructural
implícita al eliminar $\sim$524\,K parámetros del cabezal.

\begin{table*}[!t]
\centering
\caption{Cabezales y conteo de parámetros de las dos variantes
del Modelo~2.}
\label{tab:arq-m2-cabezales}
\renewcommand{\arraystretch}{1.15}
\footnotesize
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{Componente} & \textbf{DeepCNN-BN} & \textbf{DeepCNN-BN-GAP} \\
\midrule
AdaptiveAvgPool2D    & $(2,2)$           & $(1,1)$ \\
Salida del pool      & $(512, 2, 2)$     & $(512, 1, 1)$ \\
Flatten              & $2\,048$          & $512$ \\
Linear oculta        & $2048{\to}256$    & --- \\
BatchNorm1D          & $256$             & --- \\
Dropout              & $0{,}5$           & $0{,}5$ \\
Linear salida        & $256{\to}4$       & $512{\to}4$ \\
\midrule
\textbf{Params backbone}  & $4\,716\,192$ & $4\,716\,192$ \\
\textbf{Params cabezal}   & $524\,100$    & $2\,052$ \\
\textbf{Total entrenable} & $\mathbf{5\,240\,292}$ & $\mathbf{4\,716\,260}$ \\
\bottomrule
\end{tabular}
\end{table*}

\subsubsection*{Justificación de las decisiones de diseño}

\begin{itemize}
  \item \textbf{Profundidad ($10$ capas \texttt{Conv2D}).} El
        baseline emplea $3$ capas \texttt{Conv2D}; el Modelo~2
        triplica con creces esa cifra al apilar dos
        \texttt{Conv2D} por bloque (estilo VGG). Esto permite
        un campo receptivo efectivo mucho mayor sin recurrir a
        \textit{kernels} grandes, manteniendo el bajo conteo
        de parámetros por capa típico de las
        \texttt{Conv2D} $3{\times}3$.
  \item \textbf{\textit{Batch Normalization} en cada
        \texttt{Conv2D}.} A diferencia de OkanNet, donde BN se
        limitaba a $3$ ubicaciones, aquí aparece tras cada una
        de las $10$ convoluciones. La técnica reduce el
        \textit{internal covariate shift} y permite trabajar con
        \textit{learning rates} relativamente altos, pero su
        eficacia depende de un \textit{batch} suficientemente
        grande: véase Sección~\ref{sec:hiperparam} para el
        ajuste de \textit{batch size}.
  \item \textbf{\textit{Dropout2D} con tasa creciente
        ($0{,}10 \to 0{,}30$).} El uso de \textit{Dropout2D}
        ---que elimina canales completos en lugar de elementos
        individuales--- es más apropiado que el \textit{Dropout}
        clásico en capas convolucionales, donde los píxeles
        vecinos están altamente correlacionados~\cite{tompson2015efficient}.
        La progresión creciente refleja que las capas profundas
        son más propensas al sobreajuste por su mayor
        especialización.
  \item \textbf{\texttt{Conv2D} sin sesgo.} Las capas
        \texttt{Conv2D} se configuran con \texttt{bias=False}
        porque el sesgo es redundante cuando va seguido de
        \texttt{BatchNorm}, que ya aprende un parámetro de
        desplazamiento por canal.
  \item \textbf{Inicialización \textit{Kaiming Normal}.} Se
        aplica la misma estrategia que en el Modelo~1
        ---\texttt{fan\_out} en \texttt{Conv2D},
        \texttt{fan\_in} en \texttt{Linear}--- por la misma
        razón: el cabezal FC denso de DeepCNN-BN
        (\texttt{Linear(2048, 256)}) podría divergir bajo
        \texttt{fan\_out} si la magnitud de preactivaciones se
        amplifica con BN.
  \item \textbf{Dos cabezales contrastables.} La hipótesis
        implícita es que, con un \textit{backbone} ya profundo
        y regularizado, el FC denso de DeepCNN-BN no
        necesariamente aporta capacidad discriminativa real
        sobre el GAP. La comparación entre ambas variantes,
        manteniendo idénticos hiperparámetros, permite verificar
        empíricamente esta hipótesis.
\end{itemize}

El número total de parámetros entrenables es $5\,240\,292$
para DeepCNN-BN y $4\,716\,260$ para DeepCNN-BN-GAP. Ambos
valores están por debajo de los $6\,446\,756$ del baseline
pese a una profundidad $\sim 3{,}3\times$ mayor, lo cual ilustra
el aporte estructural de las \texttt{Conv2D} $3{\times}3$
encadenadas frente al cabezal FC denso dominante en el
baseline.

\subsection{Arquitectura Modelo 3 --- MRIResNet con Atención SE}
\label{sec:arq-m3}

El Modelo~3, denominado \textbf{MRIResNet}, extiende el
\textit{baseline} en tres ejes: (i)~un preprocesamiento multimodal
de cinco canales que enriquece la representación de entrada,
(ii)~bloques residuales con conexiones \textit{skip} que mitigan
la degradación del gradiente en redes profundas, y (iii)~módulos
de atención \textit{Squeeze-and-Excitation} (SE)~\cite{hu2018senet}
para la priorización adaptativa de características.

\subsubsection*{Preprocesamiento multimodal (5 canales)}

En lugar de la representación RGB $(B,3,224,224)$, el Modelo~3
recibe un tensor $(B,5,224,224)$ generado mediante:

\begin{enumerate}
  \item \textbf{Conversión RGB y redimensionado} a $224{\times}224$.
  \item \textbf{CLAHE} (\textit{Contrast Limited Adaptive Histogram
        Equalization}, $\text{clipLimit}{=}2{,}0$,
        $\text{tileGrid}{=}8{\times}8$): ecualización local del
        histograma con límite de contraste, que mejora la visibilidad
        de bordes tumorales sin amplificar el ruido global.
  \item \textbf{Sobel}: gradiente espacial
        $G {=} \sqrt{G_x^2{+}G_y^2}$, normalizado a $[0,1]$
        y aplicado sobre la imagen CLAHE para maximizar la calidad
        de los bordes extraídos.
  \item \textbf{Fusión}: concatenación de los canales
        $[\,R,\,G,\,B,\,\text{CLAHE},\,\text{Sobel}\,]$.
  \item \textbf{Normalización canal a canal} con estadísticos
        $(\mu_c, \sigma_c)$ estimados sobre el conjunto
        \textit{Training}.
\end{enumerate}

Este diseño está conceptualmente inspirado en los hallazgos de
Hubel y Wiesel~\cite{hubel1959receptive} sobre la selectividad
orientacional de las neuronas de la corteza visual primaria, cuya
sensibilidad a bordes y orientaciones motivó el diseño de los
primeros filtros convolucionales aprendibles.

\subsubsection*{Bloques residuales}

Cada bloque residual aprende la transformación
$\mathcal{F}(\mathbf{x}) {+} \mathbf{x}$~\cite{he2016resnet},
con estructura interna:
\texttt{Conv}$(3{\times}3)$--\texttt{BN}--\texttt{ReLU}--%
\texttt{Conv}$(3{\times}3)$--\texttt{BN}$\oplus\mathbf{x}$--%
\texttt{ReLU}.
Cuando el número de canales entre entrada y salida difiere,
el camino \textit{skip} incorpora una proyección
\texttt{Conv}$(1{\times}1)${+}\texttt{BN}
para alinear dimensiones sin parámetros adicionales
significativos.

\subsubsection*{Módulo Squeeze-and-Excitation}

El módulo SE recalibra los canales en tres etapas:

\begin{enumerate}
  \item \textbf{Squeeze}: \textit{Global Average Pooling}
        $z_c = \frac{1}{H{\times}W}\sum_{i,j} x_c(i,j)$
        $\to$ descriptor $\mathbf{z}\in\mathbb{R}^C$.
  \item \textbf{Excitation}: \texttt{FC}--\texttt{ReLU}--%
        \texttt{FC}--\texttt{Sigmoid} $\to$ pesos por canal
        $\mathbf{s}\in[0,1]^C$, con ratio de reducción $r{=}8$.
  \item \textbf{Reescalado}: $\hat{x}_c = s_c \cdot x_c$,
        amplificando características relevantes y suprimiendo
        las irrelevantes.
\end{enumerate}

\begin{table*}[!t]
\centering
\caption{Arquitectura del Modelo~3 (MRIResNet). Entrada
$(B,5,224,224)$. ResBlock($a{\to}b$): bloque residual con $a$
canales de entrada y $b$ de salida. SE: módulo
Squeeze-and-Excitation con $r{=}8$.}
\label{tab:arq-m3}
\renewcommand{\arraystretch}{1.2}
\setlength{\tabcolsep}{6pt}
\footnotesize
\begin{tabular}{@{}clcc@{}}
\toprule
\textbf{Etapa} & \textbf{Componentes} &
\textbf{Salida $(C,H,W)$} & \textbf{Params} \\
\midrule
Inicial &
  Conv$(5{\to}32,3{\times}3)$+BN+ReLU+MaxPool &
  $(32,112,112)$ & $1\,504$ \\
Etapa~1 &
  2$\times$[ResBlock$(32{\to}32)$+SE]+MaxPool &
  $(32,56,56)$ & $37\,704$ \\
Etapa~2 &
  2$\times$[ResBlock$(32{\to}64)$+SE]+MaxPool &
  $(64,28,28)$ & $133\,904$ \\
Etapa~3 &
  2$\times$[ResBlock$(64{\to}128)$+SE]+MaxPool &
  $(128,14,14)$ & $534\,048$ \\
Etapa~4 &
  ResBlock$(128{\to}256)$+SE &
  $(256,14,14)$ & $935\,712$ \\
GAP &
  \texttt{AdaptiveAvgPool2d(1)} &
  $(256,1,1)$ & $0$ \\
Cabezal &
  Linear$(256{\to}128)$+ReLU+Drop$(0{,}5)$+Linear$(128{\to}4)$ &
  $(4,)$ & $33\,412$ \\
\midrule
\multicolumn{3}{r}{\textbf{Total parámetros entrenables:}} &
  $\mathbf{1\,676\,284}$ \\
\bottomrule
\end{tabular}
\end{table*}

Las decisiones de diseño se justifican como sigue:

\begin{itemize}
  \item \textbf{Entrada de 5 canales}: la primera convolución acepta
        \texttt{Conv2d(5,\,32,\,3)} en lugar de 3 canales, procesando
        simultáneamente textura RGB, contraste local (CLAHE) y
        gradientes estructurales (Sobel).
  \item \textbf{Cuatro etapas residuales} con canales
        $32{\to}64{\to}128{\to}256$, siguiendo la progresión
        geométrica de ResNet~\cite{he2016resnet}.
        Las tres primeras etapas aplican
        \textit{MaxPool}~$2{\times}2$; la cuarta preserva la
        resolución $14{\times}14$ para que el GAP agregue
        información espacial de mayor granularidad.
  \item \textbf{Global Average Pooling} reemplaza el cabezal
        \texttt{Flatten\,$\to$\,Linear(50\,176,\,128)} del Modelo~1,
        reduciendo los parámetros de $6{,}44M$ a $1{,}68M$
        (${\approx}74$\,\% menos) y mejorando la robustez espacial.
  \item \textbf{Módulos SE con $r{=}8$}: el ratio garantiza al menos
        4 unidades ocultas en las etapas de 32 canales
        ($32/8{=}4$), equilibrando capacidad expresiva y coste
        computacional.
  \item \textbf{Inicialización Kaiming}: idéntica al Modelo~1,
        con modo \texttt{fan\_out} para \texttt{Conv2d} y
        \texttt{fan\_in} para \texttt{Linear}.
  \item \textbf{\textit{Dropout}~(0{,}5)} aplicado únicamente en el
        cabezal, evitando interrumpir el flujo residual dentro de
        los bloques~\cite{he2016resnet}.
\end{itemize}

El total de parámetros entrenables es de $1\,676\,284$, frente a
los $6\,446\,756$ del Modelo~1. La diferencia se debe
exclusivamente al reemplazo del cabezal denso por GAP: el bloque
convolucional resultante es considerablemente más profundo
($4$ etapas residuales vs.\ $3$ bloques simples) pero
paramé\-tricamente más eficiente.
